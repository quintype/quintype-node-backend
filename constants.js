const DEFAULT_LIMIT = '10';

const DEFAULT_DEPTH = 1;

const DEFAULT_STORY_FIELDS = "headline,subheadline,slug,url,hero-image-s3-key,hero-image-caption,hero-image-metadata,first-published-at,last-published-at,alternative,published-at,authors,author-name,author-id,sections,story-template,metadata";

module.exports = {DEFAULT_DEPTH, DEFAULT_LIMIT, DEFAULT_STORY_FIELDS};
